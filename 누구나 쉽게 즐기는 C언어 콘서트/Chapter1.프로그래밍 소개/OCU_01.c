#include <stdio.h>	// 표준 입출력 함수가 선언된 헤더파일을 포함

int main(void)
{
	printf("HELLO! My first C program.\n\r");				// \n : 다음 줄로 줄바꿈(Line feed), \r : 그 줄의 처음으로 이동(Carriage return)
	printf("김수연 goes for a good C programmer.\n\r");		// 큰 따옴표 안에 있는 문자열을 출력
	return(0);	// 정상종료
}

